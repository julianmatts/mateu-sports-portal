# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Netlify (local)
.netlify/

# Cloudflare (estado local de `wrangler pages dev`: KV/D1 simulados)
.wrangler/

# Temporales
*.log
*.tmp
~$*

# Salida local del ETL de Indicadores (lo versionado vive en data/indicadores/)
out/

# Proyecto aparte que vive en esta carpeta: tiene su propio repo
# (julianmatts/entregas-ruge-edlp) y su propio Worker de Cloudflare.
# Si se versiona acá queda como submodulo sin .gitmodules y el build
# del portal falla en "updating repository submodules".
ruge-edlp/
